#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main - program that prints number 0 - 9,
*Return: 0
*/

int main(void)
{
	int a
	for (a = 0; a <= 9; a++)
	{
		printf(a);

	}
	putchar(10);
	return (0);
}
