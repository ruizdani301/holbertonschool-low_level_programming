#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - prints the alphabet in lowercase
*
*Return: 0
*/
int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar(10);
	return (0);
}
